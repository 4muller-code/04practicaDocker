# ... (otras partes del workflow omitidas por brevedad)

jobs:
  security_scan:
    # ... (permisos y configuración)
    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      # --- Construcción de la imagen del Front-end (FE) ---
      # El contexto '.' es la raíz del repositorio.
      - name: 🏗️ Build Front-end Image (fe-web)
        # Asumiendo que el Dockerfile está en 'docker/fe/Dockerfile'
        run: docker build -t fe-web-image -f docker/fe/Dockerfile . 

      - name: 🔎 Scan Front-end Image with Trivy
        # ... (rest of the step)

      # ... (upload SARIF)

      # --- Construcción de la imagen del Task Worker (BE) ---
      # El contexto '.' es la raíz del repositorio.
      - name: 🏗️ Build Task Worker Image
        # Asumiendo que el Dockerfile está en 'docker/worker/Dockerfile-php'
        run: docker build -t task-worker-image -f docker/worker/Dockerfile-php .

      - name: 🔎 Scan Task Worker Image with Trivy
        # ... (rest of the step)

      # ... (rest of the job)

  # ... (resto del workflow)
